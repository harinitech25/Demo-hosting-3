<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Harini — Portfolio</title>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,500&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

<style>

:root{
  --bg:#0b0b0f;
  --bg-soft:#121218;
  --gold:#c9a24b;
  --gold-soft:#e8d6a8;
  --ink:#f3f1ec;
  --muted:#9a958c;
  --line:rgba(201,162,75,.25);
}

body.light{
  --bg:#f6f3ec;
  --bg-soft:#ffffff;
  --gold:#9c7a2f;
  --gold-soft:#7a5e22;
  --ink:#1c1a16;
  --muted:#6b665c;
  --line:rgba(156,122,47,.25);
}

body.light::before{
  background:
    radial-gradient(ellipse at 20% 0%, rgba(156,122,47,.06), transparent 55%),
    radial-gradient(ellipse at 80% 100%, rgba(156,122,47,.05), transparent 55%);
}

#preloader{
  position:fixed;
  inset:0;
  background:var(--bg);
  z-index:999;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:18px;
  transition:opacity .6s ease, visibility .6s ease;
}

#preloader.hide{
  opacity:0;
  visibility:hidden;
}

.loader-mark{
  font-family:'Cormorant Garamond',serif;
  font-size:34px;
  letter-spacing:.1em;
  color:var(--ink);
}

.loader-mark span{
  color:var(--gold);
}

.loader-bar{
  width:160px;
  height:1px;
  background:var(--line);
  overflow:hidden;
  position:relative;
}

.loader-bar::after{
  content:"";
  position:absolute;
  left:-40%;
  top:0;
  width:40%;
  height:100%;
  background:var(--gold);
  animation:loaderSlide 1.1s ease-in-out infinite;
}

@keyframes loaderSlide{
  0%{left:-40%;}
  50%{left:60%;}
  100%{left:100%;}
}

#scrollProgress{
  position:fixed;
  top:0;
  left:0;
  height:2px;
  width:0%;
  background:var(--gold);
  z-index:100;
  transition:width .1s linear;
}

.theme-toggle{
  width:36px;
  height:36px;
  border:1px solid var(--line);
  border-radius:50%;
  background:transparent;
  color:var(--gold);
  cursor:pointer;
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:15px;
  transition:.3s ease;
  margin-left:30px;
}

.theme-toggle:hover{
  border-color:var(--gold);
  background:rgba(201,162,75,.08);
}

.typed-cursor{
  display:inline-block;
  width:1px;
  background:var(--gold);
  margin-left:2px;
  animation:blink 1s step-start infinite;
}

@keyframes blink{
  50%{opacity:0;}
}

.socials{
  display:flex;
  gap:18px;
  justify-content:center;
  margin-top:30px;
}

.socials a{
  width:38px;
  height:38px;
  border:1px solid var(--line);
  border-radius:50%;
  display:flex;
  align-items:center;
  justify-content:center;
  color:var(--muted);
  text-decoration:none;
  font-size:14px;
  transition:.3s ease;
}

.socials a:hover{
  border-color:var(--gold);
  color:var(--gold);
  transform:translateY(-3px);
}

.btn-row{
  display:flex;
  gap:18px;
  flex-wrap:wrap;
  justify-content:center;
}

.btn.btn-outline{
  border:1px solid var(--line);
  color:var(--muted);
}

.btn.btn-outline:hover{
  background:transparent;
  border-color:var(--gold);
  color:var(--gold-soft);
  box-shadow:none;
}

.stats{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(180px,1fr));
  gap:24px;
  max-width:1000px;
  margin:auto;
  text-align:center;
}

.stats .reveal{
  background:var(--bg-soft);
  border:1px solid var(--line);
  border-radius:10px;
  padding:34px 18px;
  transition:.4s ease;
}

.stats .reveal:hover{
  border-color:var(--gold);
  transform:translateY(-4px);
}

.stat-number{
  font-family:'Cormorant Garamond',serif;
  font-size:54px;
  color:var(--gold);
  font-weight:600;
  text-shadow:0 0 24px rgba(201,162,75,.2);
}

.stat-label{
  margin-top:8px;
  font-size:12px;
  letter-spacing:.18em;
  text-transform:uppercase;
  color:var(--muted);
}

#backToTop{
  position:fixed;
  bottom:30px;
  right:30px;
  width:46px;
  height:46px;
  border-radius:50%;
  border:1px solid var(--gold);
  background:var(--bg-soft);
  color:var(--gold);
  cursor:pointer;
  font-size:16px;
  display:flex;
  align-items:center;
  justify-content:center;
  opacity:0;
  visibility:hidden;
  transform:translateY(10px);
  transition:.35s ease;
  z-index:60;
}

#backToTop.show{
  opacity:1;
  visibility:visible;
  transform:translateY(0);
}

#backToTop:hover{
  background:var(--gold);
  color:#0b0b0f;
}

#toast{
  position:fixed;
  bottom:30px;
  left:50%;
  transform:translateX(-50%) translateY(20px);
  background:var(--bg-soft);
  border:1px solid var(--gold);
  color:var(--ink);
  padding:14px 28px;
  border-radius:2px;
  font-size:13px;
  letter-spacing:.05em;
  opacity:0;
  visibility:hidden;
  transition:.4s ease;
  z-index:200;
}

#toast.show{
  opacity:1;
  visibility:visible;
  transform:translateX(-50%) translateY(0);
}

.field-error{
  font-size:12px;
  color:#d96b6b;
  margin-top:-10px;
  display:none;
}

.field-error.show{
  display:block;
}

*{
  margin:0;
  padding:0;
  box-sizing:border-box;
}

html{
  scroll-behavior:smooth;
}

body{
  font-family:'Inter',sans-serif;
  background:var(--bg);
  color:var(--ink);
  overflow-x:hidden;
  position:relative;
  transition:background .5s ease, color .5s ease;
}

body::before{
  content:"";
  position:fixed;
  inset:0;
  background:
    radial-gradient(ellipse at 20% 0%, rgba(201,162,75,.08), transparent 55%),
    radial-gradient(ellipse at 80% 100%, rgba(201,162,75,.06), transparent 55%);
  pointer-events:none;
  z-index:0;
  animation:ambientDrift 18s ease-in-out infinite alternate;
}

@keyframes ambientDrift{
  0%{background-position:0% 0%, 0% 0%; filter:brightness(1);}
  50%{background-position:8% 6%, -6% -8%; filter:brightness(1.12);}
  100%{background-position:-6% 10%, 8% -4%; filter:brightness(1);}
}

#auraCanvas{
  position:fixed;
  inset:0;
  z-index:0;
  pointer-events:none;
  opacity:.55;
}

#bgWaves{
  position:fixed;
  inset:0;
  z-index:0;
  pointer-events:none;
  opacity:.5;
}

#trailCanvas{
  position:fixed;
  inset:0;
  z-index:2;
  pointer-events:none;
}

@media(max-width:768px){
  #trailCanvas{display:none;}
}

.particle{
  position:fixed;
  top:100%;
  border-radius:50%;
  background:var(--gold);
  pointer-events:none;
  z-index:1;
  opacity:0;
  animation:floatUp linear infinite;
  box-shadow:0 0 6px rgba(201,162,75,.8);
}

@keyframes floatUp{
  0%{transform:translateY(0) translateX(0); opacity:0;}
  8%{opacity:.7;}
  92%{opacity:.5;}
  100%{transform:translateY(-110vh) translateX(var(--drift,30px)); opacity:0;}
}

section{
  padding:140px 8%;
  position:relative;
  z-index:1;
}

/* ---- nav ---- */

nav{
  position:fixed;
  top:0;
  left:0;
  right:0;
  z-index:50;
  display:flex;
  justify-content:space-between;
  align-items:center;
  padding:22px 8%;
  background:rgba(11,11,15,.5);
  backdrop-filter:blur(18px) saturate(140%);
  border-bottom:1px solid transparent;
  transition:border-color .4s ease, background .4s ease, padding .4s ease;
}

nav.scrolled{
  border-bottom:1px solid var(--line);
  background:rgba(11,11,15,.88);
  padding:16px 8%;
}

.logo{
  font-family:'Cormorant Garamond',serif;
  font-size:25px;
  letter-spacing:.08em;
  color:var(--ink);
  position:relative;
}

.logo span{
  color:var(--gold);
  text-shadow:0 0 16px rgba(201,162,75,.5);
}

.nav-links{
  display:flex;
  gap:38px;
  list-style:none;
}

.nav-links a{
  text-decoration:none;
  color:var(--muted);
  font-size:13px;
  letter-spacing:.12em;
  text-transform:uppercase;
  transition:color .3s ease;
  position:relative;
}

.nav-links a::after{
  content:"";
  position:absolute;
  left:0;
  bottom:-6px;
  width:0;
  height:1px;
  background:var(--gold);
  transition:width .3s ease;
}

.nav-links a:hover{
  color:var(--gold-soft);
}

.nav-links a.active{
  color:var(--gold);
}

.nav-links a:hover::after{
  width:100%;
}

.nav-toggle{
  display:none;
  flex-direction:column;
  gap:5px;
  cursor:pointer;
  background:none;
  border:none;
}

.nav-toggle span{
  width:24px;
  height:1px;
  background:var(--ink);
}

/* ---- eyebrow / title ---- */

.eyebrow{
  display:flex;
  align-items:center;
  justify-content:center;
  gap:14px;
  font-size:12px;
  letter-spacing:.3em;
  text-transform:uppercase;
  color:var(--gold);
  margin-bottom:18px;
}

.eyebrow::before,
.eyebrow::after{
  content:"";
  width:36px;
  height:1px;
  background:var(--line);
}

.title{
  text-align:center;
  font-size:46px;
  font-weight:500;
  font-family:'Cormorant Garamond',serif;
  margin-bottom:60px;
  color:var(--ink);
  position:relative;
}

.title::after{
  content:"";
  display:block;
  width:46px;
  height:2px;
  background:var(--gold);
  margin:18px auto 0;
  border-radius:2px;
}

/* ---- hero ---- */

.hero{
  height:100vh;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
  text-align:center;
  padding:0 8%;
}

.hero-kicker{
  font-size:13px;
  letter-spacing:.4em;
  text-transform:uppercase;
  color:var(--gold);
  margin-bottom:28px;
  opacity:0;
  animation:fadeUp 1s .2s ease forwards;
}

.hero h1{
  font-size:clamp(64px,10vw,128px);
  font-family:'Cormorant Garamond',serif;
  font-weight:500;
  line-height:1;
  color:var(--ink);
  opacity:0;
  animation:fadeUp 1.1s .4s ease forwards;
}

.hero h1 em{
  font-style:italic;
  color:var(--gold);
  animation:emGlow 3.5s ease-in-out infinite;
}

@keyframes emGlow{
  0%,100%{text-shadow:0 0 18px rgba(201,162,75,.25);}
  50%{text-shadow:0 0 34px rgba(201,162,75,.55);}
}

.hero p{
  font-size:17px;
  margin-top:22px;
  color:var(--muted);
  letter-spacing:.04em;
  opacity:0;
  animation:fadeUp 1.1s .65s ease forwards;
}

.hero .divider{
  width:1px;
  height:60px;
  background:linear-gradient(var(--gold),transparent);
  margin:36px auto 0;
  opacity:0;
  animation:fadeUp 1.1s .85s ease forwards;
}

@keyframes fadeUp{
  from{opacity:0; transform:translateY(22px);}
  to{opacity:1; transform:translateY(0);}
}

.btn{
  display:inline-block;
  margin-top:38px;
  padding:16px 44px;
  border-radius:2px;
  text-decoration:none;
  background:transparent;
  border:1px solid var(--gold);
  color:var(--gold-soft);
  font-size:13px;
  letter-spacing:.18em;
  text-transform:uppercase;
  transition:.4s ease;
  opacity:0;
  animation:fadeUp 1.1s 1.05s ease forwards;
  position:relative;
  overflow:hidden;
  isolation:isolate;
}

.btn::before{
  content:"";
  position:absolute;
  inset:0;
  background:linear-gradient(120deg, transparent 30%, rgba(255,255,255,.35) 50%, transparent 70%);
  transform:translateX(-130%);
  transition:transform .6s ease;
  z-index:-1;
}

.btn:hover::before{
  transform:translateX(130%);
}

.btn:hover{
  background:var(--gold);
  color:#0b0b0f;
  box-shadow:0 0 30px rgba(201,162,75,.45);
  transform:translateY(-2px);
}

/* ---- reveal on scroll ---- */

.reveal{
  opacity:0;
  transform:translateY(34px) scale(.97);
  filter:blur(6px);
  transition:opacity 1s cubic-bezier(.2,.8,.2,1), transform 1s cubic-bezier(.2,.8,.2,1), filter 1s ease;
}

.reveal.in{
  opacity:1;
  transform:translateY(0) scale(1);
  filter:blur(0);
}

/* ---- card ---- */

.card{
  background:linear-gradient(180deg, rgba(255,255,255,.025), rgba(255,255,255,0)), var(--bg-soft);
  border:1px solid var(--line);
  border-radius:10px;
  padding:46px;
  position:relative;
  overflow:hidden;
  isolation:isolate;
  transition:border-color .5s ease, box-shadow .5s ease, transform .5s ease;
  backdrop-filter:blur(6px);
}

.card::before{
  content:"";
  position:absolute;
  inset:-1px;
  padding:1px;
  border-radius:inherit;
  background:linear-gradient(135deg, rgba(201,162,75,.5), transparent 35%, transparent 65%, rgba(201,162,75,.3));
  -webkit-mask:linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  -webkit-mask-composite:xor;
  mask-composite:exclude;
  pointer-events:none;
  z-index:-1;
  opacity:.7;
}

.card:hover{
  border-color:var(--gold);
  box-shadow:0 24px 60px rgba(0,0,0,.4);
  transform:translateY(-3px);
}

.about{
  text-align:center;
  max-width:780px;
  margin:auto;
}

.about p{
  color:var(--muted);
  font-size:17px;
  line-height:1.9;
  margin-bottom:20px;
}

.about p:first-of-type{
  color:var(--ink);
  font-size:19px;
}

.profile{
  width:200px;
  height:200px;
  border-radius:50%;
  object-fit:cover;
  border:1px solid var(--gold);
  padding:6px;
  margin-bottom:30px;
  box-shadow:0 0 0 1px var(--line), 0 20px 50px rgba(0,0,0,.4), 0 0 40px rgba(201,162,75,.15);
}

/* ---- skills ---- */

.skills{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(170px,1fr));
  gap:14px;
  max-width:1100px;
  margin:auto;
}

.skill{
  background:var(--bg-soft);
  padding:18px 16px;
  border-radius:8px;
  text-align:center;
  border:1px solid var(--line);
  font-size:14px;
  letter-spacing:.03em;
  color:var(--muted);
  transition:.4s cubic-bezier(.2,.8,.2,1);
  position:relative;
  overflow:hidden;
  isolation:isolate;
}

.skill::before{
  content:"";
  position:absolute;
  inset:0;
  background:linear-gradient(120deg, transparent 30%, rgba(201,162,75,.18) 50%, transparent 70%);
  transform:translateX(-120%);
  transition:transform .7s ease;
  z-index:-1;
}

.skill:hover::before{
  transform:translateX(120%);
}

.skill:hover{
  transform:translateY(-5px) scale(1.02);
  border-color:var(--gold);
  color:var(--gold-soft);
  box-shadow:0 14px 28px rgba(0,0,0,.4);
}

/* ---- projects ---- */

.project{
  background:var(--bg-soft);
  padding:38px 32px;
  border-radius:10px;
  border:1px solid var(--line);
  border-top:2px solid var(--gold);
  transition:.5s cubic-bezier(.2,.8,.2,1);
  position:relative;
  overflow:hidden;
  isolation:isolate;
  counter-increment:project-count;
}

.project::after{
  content:"0" counter(project-count);
  position:absolute;
  top:14px;
  right:20px;
  font-family:'Cormorant Garamond',serif;
  font-size:34px;
  color:var(--gold);
  opacity:.18;
  font-weight:600;
}

.projects{
  counter-reset:project-count;
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(280px,1fr));
  gap:24px;
  max-width:1200px;
  margin:auto;
}

.project::before{
  content:"";
  position:absolute;
  inset:0;
  background:linear-gradient(125deg, transparent 35%, rgba(201,162,75,.14) 50%, transparent 65%);
  transform:translateX(-130%);
  transition:transform .8s ease;
  z-index:-1;
}

.project:hover::before{
  transform:translateX(130%);
}

.project:hover{
  transform:translateY(-8px) scale(1.01);
  box-shadow:0 22px 44px rgba(0,0,0,.45);
}

.project h3{
  font-family:'Cormorant Garamond',serif;
  font-size:24px;
  font-weight:600;
  color:var(--ink);
  margin-bottom:14px;
}

.project p{
  color:var(--muted);
  font-size:15px;
  line-height:1.7;
}

/* ---- timeline ---- */

.timeline{
  max-width:760px;
  margin:auto;
  position:relative;
  padding-left:30px;
  border-left:1px solid var(--line);
}

.timeline-item{
  margin-bottom:36px;
  position:relative;
  padding-left:30px;
}

.timeline-item::before{
  content:"";
  position:absolute;
  left:-44px;
  top:6px;
  width:9px;
  height:9px;
  border-radius:50%;
  background:var(--gold);
  box-shadow:0 0 0 4px rgba(201,162,75,.15);
}

.timeline-item h3{
  font-family:'Cormorant Garamond',serif;
  font-size:22px;
  font-weight:600;
  color:var(--ink);
  margin-bottom:6px;
}

.timeline-item p{
  color:var(--muted);
  font-size:15px;
}

/* ---- achievements ---- */

.achievements{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(240px,1fr));
  gap:20px;
  max-width:1100px;
  margin:auto;
}

.achievement{
  padding:36px 28px;
  border-radius:10px;
  background:var(--bg-soft);
  border:1px solid var(--line);
  text-align:center;
  transition:.4s ease;
}

.achievement:hover{
  border-color:var(--gold);
  transform:translateY(-4px);
  box-shadow:0 16px 32px rgba(0,0,0,.35);
}

.achievement h3{
  font-family:'Cormorant Garamond',serif;
  font-size:21px;
  color:var(--gold-soft);
  margin-bottom:10px;
}

.achievement p{
  color:var(--muted);
  font-size:14px;
  line-height:1.6;
}

/* ---- hobbies ---- */

.hobbies{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(170px,1fr));
  gap:14px;
  max-width:1000px;
  margin:auto;
}

.hobby{
  padding:20px;
  text-align:center;
  background:var(--bg-soft);
  border:1px solid var(--line);
  border-radius:8px;
  font-size:14px;
  color:var(--muted);
  transition:.35s ease;
}

.hobby:hover{
  color:var(--gold-soft);
  border-color:var(--gold);
  transform:translateY(-3px);
}

/* ---- testimonials ---- */

.testimonial-wrap{
  max-width:760px;
  margin:auto;
  position:relative;
}

.testimonial-track{
  position:relative;
  min-height:230px;
}

.testimonial-slide{
  position:absolute;
  inset:0;
  opacity:0;
  visibility:hidden;
  transform:translateY(14px);
  transition:opacity .6s ease, transform .6s ease, visibility .6s;
  text-align:center;
}

.testimonial-slide.active{
  opacity:1;
  visibility:visible;
  transform:translateY(0);
  position:relative;
}

.testimonial-quote{
  font-family:'Cormorant Garamond',serif;
  font-style:italic;
  font-size:21px;
  line-height:1.7;
  color:var(--ink);
}

.testimonial-quote::before{
  content:"\201C";
  color:var(--gold);
  font-size:30px;
  margin-right:4px;
}

.testimonial-person{
  margin-top:24px;
}

.testimonial-name{
  color:var(--gold);
  font-size:13px;
  letter-spacing:.14em;
  text-transform:uppercase;
}

.testimonial-role{
  color:var(--muted);
  font-size:12px;
  margin-top:4px;
}

.testimonial-controls{
  display:flex;
  align-items:center;
  justify-content:center;
  gap:22px;
  margin-top:32px;
}

.testimonial-arrow{
  width:38px;
  height:38px;
  border:1px solid var(--line);
  border-radius:50%;
  background:transparent;
  color:var(--gold);
  cursor:pointer;
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:14px;
  transition:.3s ease;
}

.testimonial-arrow:hover{
  border-color:var(--gold);
  background:rgba(201,162,75,.08);
}

.testimonial-dots{
  display:flex;
  gap:9px;
}

.testimonial-dot{
  width:7px;
  height:7px;
  border-radius:50%;
  background:var(--line);
  border:none;
  cursor:pointer;
  padding:0;
  transition:.3s ease;
}

.testimonial-dot.active{
  background:var(--gold);
  transform:scale(1.3);
}

/* ---- contact ---- */

.contact{
  max-width:640px;
  margin:auto;
}

.contact-info{
  display:flex;
  flex-direction:column;
  gap:10px;
  margin-bottom:30px;
  text-align:center;
}

.contact-info p{
  color:var(--muted);
  font-size:15px;
  letter-spacing:.02em;
}

.contact-info p span{
  color:var(--gold);
  letter-spacing:.1em;
  text-transform:uppercase;
  font-size:11px;
  margin-right:10px;
}

.contact form{
  display:flex;
  flex-direction:column;
  gap:16px;
}

input,textarea{
  padding:16px 18px;
  border:1px solid var(--line);
  border-radius:8px;
  outline:none;
  background:rgba(255,255,255,.02);
  color:var(--ink);
  font-family:'Inter',sans-serif;
  font-size:14px;
  transition:border-color .3s ease, box-shadow .3s ease;
}

input::placeholder,textarea::placeholder{
  color:var(--muted);
}

input:focus,textarea:focus{
  border-color:var(--gold);
  box-shadow:0 0 0 3px rgba(201,162,75,.12);
}

textarea{
  height:130px;
  resize:none;
}

button{
  padding:16px;
  border:1px solid var(--gold);
  border-radius:8px;
  background:transparent;
  color:var(--gold-soft);
  font-size:13px;
  letter-spacing:.18em;
  text-transform:uppercase;
  cursor:pointer;
  transition:.35s ease;
}

button:hover{
  background:var(--gold);
  color:#0b0b0f;
  box-shadow:0 0 26px rgba(201,162,75,.35);
}

footer{
  text-align:center;
  padding:36px;
  border-top:1px solid var(--line);
  color:var(--muted);
  font-size:13px;
  letter-spacing:.04em;
  position:relative;
  z-index:1;
}

@media(max-width:768px){

  .nav-links{
    position:fixed;
    top:64px;
    right:0;
    flex-direction:column;
    background:rgba(11,11,15,.97);
    width:220px;
    padding:24px;
    gap:22px;
    transform:translateX(100%);
    transition:transform .4s ease;
    border-left:1px solid var(--line);
  }

  .nav-links.open{
    transform:translateX(0);
  }

  .nav-toggle{
    display:flex;
  }

  section{
    padding:90px 7%;
  }

  .title{
    font-size:32px;
  }

  .profile{
    width:160px;
    height:160px;
  }

}

/* ===== WhatsApp Floating Button ===== */
#whatsappBtn{
  position:fixed;
  bottom:30px;
  left:30px;
  width:56px;
  height:56px;
  border-radius:50%;
  background:#25D366;
  color:#fff;
  display:flex;
  align-items:center;
  justify-content:center;
  text-decoration:none;
  font-size:28px;
  z-index:150;
  box-shadow:0 0 18px rgba(37,211,102,.45);
  transition:.35s ease;
}

#whatsappBtn:hover{
  transform:translateY(-3px) scale(1.05);
  box-shadow:0 0 26px rgba(37,211,102,.65);
}

@media (max-width:768px){
  #whatsappBtn{
    width:50px;
    height:50px;
    bottom:24px;
    left:18px;
    font-size:24px;
  }
}

/* ===== Arc Reactor Glow (hero background accent) ===== */
.arc-reactor{
  position:absolute;
  top:50%;
  left:50%;
  width:520px;
  height:520px;
  transform:translate(-50%,-50%);
  pointer-events:none;
  z-index:0;
  opacity:.55;
}

.arc-reactor .ring{
  position:absolute;
  inset:0;
  border-radius:50%;
  border:1px solid rgba(120,200,255,.35);
  box-shadow:0 0 40px rgba(80,180,255,.25), inset 0 0 30px rgba(80,180,255,.15);
  animation:arcSpin 14s linear infinite;
}

.arc-reactor .ring:nth-child(2){
  inset:60px;
  border-color:rgba(201,162,75,.4);
  box-shadow:0 0 30px rgba(201,162,75,.25), inset 0 0 24px rgba(201,162,75,.18);
  animation-duration:10s;
  animation-direction:reverse;
}

.arc-reactor .ring:nth-child(3){
  inset:130px;
  border-color:rgba(120,200,255,.5);
  box-shadow:0 0 50px rgba(80,180,255,.35), inset 0 0 20px rgba(80,180,255,.2);
  animation-duration:7s;
}

.arc-reactor .core{
  position:absolute;
  inset:200px;
  border-radius:50%;
  background:radial-gradient(circle, rgba(180,225,255,.9) 0%, rgba(80,180,255,.5) 35%, rgba(80,180,255,0) 70%);
  filter:blur(2px);
  animation:arcPulse 2.6s ease-in-out infinite;
}

@keyframes arcSpin{
  from{transform:rotate(0deg);}
  to{transform:rotate(360deg);}
}

@keyframes arcPulse{
  0%,100%{opacity:.7; transform:scale(1);}
  50%{opacity:1; transform:scale(1.08);}
}

@media (max-width:768px){
  .arc-reactor{ width:320px; height:320px; }
  .arc-reactor .ring:nth-child(2){ inset:40px; }
  .arc-reactor .ring:nth-child(3){ inset:85px; }
  .arc-reactor .core{ inset:125px; }
}

/* ===== AI Chat Widget ===== */
#chatToggle{
  position:fixed;
  bottom:30px;
  right:30px;
  width:58px;
  height:58px;
  border-radius:50%;
  border:1px solid var(--gold);
  background:var(--bg-soft);
  color:var(--gold);
  cursor:pointer;
  font-size:22px;
  display:flex;
  align-items:center;
  justify-content:center;
  z-index:150;
  box-shadow:0 0 24px rgba(201,162,75,.25);
  transition:.35s ease;
}

#chatToggle:hover{
  background:var(--gold);
  color:var(--bg);
  transform:translateY(-3px);
}

#chatWindow{
  position:fixed;
  bottom:100px;
  right:30px;
  width:340px;
  max-width:88vw;
  height:460px;
  max-height:70vh;
  background:var(--bg-soft);
  border:1px solid var(--line);
  border-radius:12px;
  box-shadow:0 20px 60px rgba(0,0,0,.45);
  display:flex;
  flex-direction:column;
  overflow:hidden;
  opacity:0;
  visibility:hidden;
  transform:translateY(16px) scale(.97);
  transition:.3s ease;
  z-index:150;
}

#chatWindow.open{
  opacity:1;
  visibility:visible;
  transform:translateY(0) scale(1);
}

.chat-header{
  padding:16px 18px;
  border-bottom:1px solid var(--line);
  display:flex;
  align-items:center;
  justify-content:space-between;
  background:linear-gradient(180deg, rgba(201,162,75,.08), transparent);
}

.chat-header h4{
  font-family:'Cormorant Garamond',serif;
  font-size:19px;
  font-weight:600;
  color:var(--ink);
  letter-spacing:.03em;
}

.chat-header h4 span{
  color:var(--gold);
}

.chat-header small{
  display:block;
  font-size:10px;
  letter-spacing:.12em;
  text-transform:uppercase;
  color:var(--muted);
  margin-top:2px;
}

#chatClose{
  background:none;
  border:none;
  color:var(--muted);
  font-size:18px;
  cursor:pointer;
  line-height:1;
  padding:4px;
  transition:.25s ease;
}

#chatClose:hover{ color:var(--gold); }

#chatMessages{
  flex:1;
  overflow-y:auto;
  padding:16px;
  display:flex;
  flex-direction:column;
  gap:12px;
}

#chatMessages::-webkit-scrollbar{ width:5px; }
#chatMessages::-webkit-scrollbar-thumb{ background:var(--line); border-radius:4px; }

.chat-msg{
  max-width:85%;
  padding:10px 13px;
  border-radius:10px;
  font-size:13px;
  line-height:1.5;
}

.chat-msg.bot{
  align-self:flex-start;
  background:rgba(201,162,75,.08);
  border:1px solid var(--line);
  color:var(--ink);
}

.chat-msg.user{
  align-self:flex-end;
  background:var(--gold);
  color:#0b0b0f;
}

.chat-msg.typing{
  align-self:flex-start;
  display:flex;
  gap:4px;
  padding:12px 14px;
  background:rgba(201,162,75,.08);
  border:1px solid var(--line);
  border-radius:10px;
}

.chat-msg.typing span{
  width:6px;
  height:6px;
  border-radius:50%;
  background:var(--gold);
  animation:typingBounce 1s infinite ease-in-out;
}

.chat-msg.typing span:nth-child(2){ animation-delay:.15s; }
.chat-msg.typing span:nth-child(3){ animation-delay:.3s; }

@keyframes typingBounce{
  0%,60%,100%{ transform:translateY(0); opacity:.5; }
  30%{ transform:translateY(-4px); opacity:1; }
}

.chat-input-row{
  display:flex;
  gap:8px;
  padding:14px;
  border-top:1px solid var(--line);
}

.chat-input-row input{
  flex:1;
  background:transparent;
  border:1px solid var(--line);
  border-radius:8px;
  padding:10px 12px;
  color:var(--ink);
  font-family:'Inter',sans-serif;
  font-size:13px;
  outline:none;
  transition:.25s ease;
}

.chat-input-row input:focus{
  border-color:var(--gold);
}

.chat-input-row button{
  width:38px;
  height:38px;
  border-radius:8px;
  border:1px solid var(--gold);
  background:var(--gold);
  color:#0b0b0f;
  cursor:pointer;
  font-size:14px;
  flex-shrink:0;
  transition:.25s ease;
}

.chat-input-row button:hover{
  filter:brightness(1.1);
}

.chat-input-row button:disabled{
  opacity:.5;
  cursor:not-allowed;
}

@media(max-width:480px){
  #chatWindow{
    right:16px;
    bottom:92px;
    width:calc(100vw - 32px);
  }
  #chatToggle{
    right:16px;
    bottom:20px;
  }
}

</style>
</head>
<body>

<canvas id="bgWaves"></canvas>
<canvas id="trailCanvas"></canvas>
<div id="particleField"></div>

<div id="preloader">
<div class="loader-mark">H<span>.</span></div>
<div class="loader-bar"></div>
</div>

<div id="scrollProgress"></div>

<nav id="navbar">
<div class="logo">Harini<span>.</span></div>
<button class="nav-toggle" id="navToggle" aria-label="Toggle menu">
<span></span><span></span><span></span>
</button>
<ul class="nav-links" id="navLinks">
<li><a href="#about">About</a></li>
<li><a href="#skills">Skills</a></li>
<li><a href="#projects">Projects</a></li>
<li><a href="#education">Education</a></li>
<li><a href="#testimonials">Testimonials</a></li>
<li><a href="#contact">Contact</a></li>
</ul>
<button class="theme-toggle" id="themeToggle" aria-label="Toggle theme">&#9788;</button>
</nav>

<section class="hero" style="position:relative;">
<div class="arc-reactor">
<div class="ring"></div>
<div class="ring"></div>
<div class="ring"></div>
<div class="core"></div>
</div>
<div class="hero-kicker">Welcome to my portfolio</div>
<h1>Hari<em>ni</em></h1>
<p><span id="typedRole"></span><span class="typed-cursor">&nbsp;</span></p>
<div class="divider"></div>
<div class="btn-row">
<a href="#projects" class="btn">Explore Portfolio</a>
<a href="#contact" class="btn btn-outline">Download Resume</a>
</div>
<div class="socials">
<a href="mailto:harinitech001@email.com" aria-label="Email">@</a>
<a href="#" aria-label="LinkedIn">in</a>
<a href="#" aria-label="GitHub">gh</a>
<a href="#" aria-label="Instagram">ig</a>
</div>
</section>

<section id="about">
<div class="eyebrow">Introduction</div>
<h2 class="title">About Me</h2>

<div class="about reveal">
<p>
Hi, I'm Harini. I am passionate about technology,
web development, UI design, creativity and innovation.
</p>

<p>
I am a passionate technology enthusiast with strong interests in web development, UI/UX design, creative problem solving and innovation. I enjoy building beautiful digital experiences and transforming ideas into impactful projects.
</p>
</div>
</section>

<section id="stats">
<div class="stats">

<div class="reveal">
<div class="stat-number" data-count="12">0</div>
<div class="stat-label">Projects Built</div>
</div>

<div class="reveal">
<div class="stat-number" data-count="27">0</div>
<div class="stat-label">Skills Learned</div>
</div>

<div class="reveal">
<div class="stat-number" data-count="6">0</div>
<div class="stat-label">Certifications</div>
</div>

<div class="reveal">
<div class="stat-number" data-count="100">0</div>
<div class="stat-label">% Dedication</div>
</div>

</div>
</section>

<section id="skills">
<div class="eyebrow">Capabilities</div>
<h2 class="title">Skills</h2>

<div class="skills">

<div class="skill reveal">HTML</div>
<div class="skill reveal">CSS</div>
<div class="skill reveal">JavaScript</div>
<div class="skill reveal">React</div>
<div class="skill reveal">Web Development</div>
<div class="skill reveal">Frontend Development</div>
<div class="skill reveal">Backend Basics</div>
<div class="skill reveal">UI Design</div>
<div class="skill reveal">UX Design</div>
<div class="skill reveal">Responsive Design</div>
<div class="skill reveal">Python</div>
<div class="skill reveal">AI Tools</div>
<div class="skill reveal">Graphic Design</div>
<div class="skill reveal">Canva</div>
<div class="skill reveal">App Design</div>
<div class="skill reveal">Video Editing</div>
<div class="skill reveal">Photography</div>
<div class="skill reveal">Content Creation</div>
<div class="skill reveal">Leadership</div>
<div class="skill reveal">Communication</div>
<div class="skill reveal">Team Work</div>
<div class="skill reveal">Problem Solving</div>
<div class="skill reveal">Research Skills</div>
<div class="skill reveal">Project Management</div>
<div class="skill reveal">Creative Thinking</div>
<div class="skill reveal">Innovation</div>
<div class="skill reveal">Adaptability</div>
<div class="skill reveal">Digital Marketing</div>

</div>
</section>

<section id="projects">
<div class="eyebrow">Selected Work</div>
<h2 class="title">Projects</h2>

<div class="projects">

<div class="project reveal">
<h3>Smart Bus Tracking App</h3>
<p>Real-time tracking solution with live bus location, ETA prediction and route management.</p>
</div>

<div class="project reveal">
<h3>Smart Fridge AI</h3>
<p>AI-based application that suggests recipes using available ingredients.</p>
</div>

<div class="project reveal">
<h3>Portfolio Website</h3>
<p>Modern personal website showcasing skills, projects and achievements.</p>
</div>

<div class="project reveal">
<h3>Student Management System</h3>
<p>Simple web-based platform for managing student records and attendance.</p>
</div>

</div>
</section>

<section id="education">
<div class="eyebrow">Journey</div>
<h2 class="title">Education</h2>

<div class="timeline">

<div class="timeline-item reveal">
<h3>Bachelor Degree</h3>
<p>Computer Science / Information Technology</p>
</div>

<div class="timeline-item reveal">
<h3>Web Development Training</h3>
<p>HTML, CSS, JavaScript and UI Design</p>
</div>

<div class="timeline-item reveal">
<h3>AI &amp; Innovation Learning</h3>
<p>Exploring Artificial Intelligence and emerging technologies</p>
</div>

</div>
</section>

<section id="achievements">
<div class="eyebrow">Recognition</div>
<h2 class="title">Achievements</h2>

<div class="achievements">

<div class="achievement reveal">
<h3>Web Development</h3>
<p>Built responsive websites and portfolios.</p>
</div>

<div class="achievement reveal">
<h3>Creative Design</h3>
<p>Designed modern UI concepts and layouts.</p>
</div>

<div class="achievement reveal">
<h3>Innovation Projects</h3>
<p>Created unique technology-based project ideas.</p>
</div>

<div class="achievement reveal">
<h3>Team Collaboration</h3>
<p>Successfully worked on collaborative projects.</p>
</div>

</div>
</section>

<section id="hobbies">
<div class="eyebrow">Beyond Work</div>
<h2 class="title">Hobbies</h2>

<div class="hobbies">

<div class="hobby reveal">Designing</div>
<div class="hobby reveal">Photography</div>
<div class="hobby reveal">Music</div>
<div class="hobby reveal">Technology Exploration</div>
<div class="hobby reveal">Content Creation</div>
<div class="hobby reveal">Learning New Skills</div>

</div>
</section>

<section id="testimonials">
<div class="eyebrow">Kind Words</div>
<h2 class="title">Testimonials</h2>

<div class="testimonial-wrap reveal">
<div class="card">

<div class="testimonial-track" id="testimonialTrack">

<div class="testimonial-slide active">
<p class="testimonial-quote">Harini turned our rough ideas into a clean, polished interface faster than we expected. Her attention to detail on every screen really stood out.</p>
<div class="testimonial-person">
<div class="testimonial-name">Praveen Kumar</div>
<div class="testimonial-role">Founder, Studio Loop</div>
</div>
</div>

<div class="testimonial-slide">
<p class="testimonial-quote">Working with Harini felt effortless. She understood the brief instantly and delivered a frontend that was both beautiful and easy to maintain.</p>
<div class="testimonial-person">
<div class="testimonial-name">Aishwarya R</div>
<div class="testimonial-role">Product Manager, Nimbus Labs</div>
</div>
</div>

<div class="testimonial-slide">
<p class="testimonial-quote">Great communicator, sharp eye for design, and always open to feedback. Our website's bounce rate dropped noticeably after her redesign.</p>
<div class="testimonial-person">
<div class="testimonial-name">Karthik S</div>
<div class="testimonial-role">Marketing Lead, Verve Digital</div>
</div>
</div>

</div>

<div class="testimonial-controls">
<button class="testimonial-arrow" id="testimonialPrev" aria-label="Previous testimonial">&#8592;</button>
<div class="testimonial-dots" id="testimonialDots"></div>
<button class="testimonial-arrow" id="testimonialNext" aria-label="Next testimonial">&#8594;</button>
</div>

</div>
</div>

</section>

<section id="contact">
<div class="eyebrow">Get In Touch</div>
<h2 class="title">Contact</h2>

<div class="contact reveal">

<div class="card">

<div class="contact-info">
<p><span>Email</span>harinitech001@email.com</p>
<p><span>Phone</span>+91 8825459492</p>
<p><span>Location</span>Erode, T N Palayam, Tamil Nadu, India</p>
</div>

<form id="contactForm" novalidate>
<input type="text" id="nameField" placeholder="Your Name">
<span class="field-error" id="nameError">Please enter your name.</span>
<input type="email" id="emailField" placeholder="Your Email">
<span class="field-error" id="emailError">Please enter a valid email.</span>
<textarea id="messageField" placeholder="Your Message"></textarea>
<span class="field-error" id="messageError">Please enter a message.</span>
<button type="submit">Send Message</button>
</form>

</div>

</div>

</section>

<button id="backToTop" aria-label="Back to top">&#8593;</button>
<div id="toast">Message sent successfully.</div>

<a id="whatsappBtn" href="https://wa.me/918825459492" target="_blank" rel="noopener" aria-label="Chat on WhatsApp">
<svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor"><path d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91c0 1.75.46 3.45 1.32 4.95L2 22l5.27-1.38a9.9 9.9 0 0 0 4.77 1.21h.01c5.46 0 9.91-4.45 9.91-9.91C21.96 6.45 17.5 2 12.04 2zm5.77 14.06c-.24.68-1.4 1.31-1.93 1.39-.5.08-1.13.11-1.83-.11-.42-.13-.96-.31-1.65-.6-2.9-1.25-4.79-4.17-4.93-4.36-.14-.19-1.18-1.57-1.18-3 0-1.42.75-2.12 1.02-2.41.27-.29.58-.36.78-.36.19 0 .39 0 .56.01.18.01.42-.07.66.5.24.58.83 2 .9 2.15.07.15.12.32.02.52-.1.19-.15.31-.3.48-.15.17-.31.38-.45.51-.15.14-.3.3-.13.59.17.29.76 1.25 1.63 2.02 1.12.99 2.06 1.3 2.35 1.45.29.14.46.12.63-.07.17-.19.72-.84.91-1.13.19-.29.38-.24.63-.14.26.1 1.65.78 1.93.92.29.14.48.21.55.33.07.12.07.68-.17 1.36z"/></svg>
</a>

<!-- AI Chat Widget -->
<button id="chatToggle" aria-label="Open chat">&#128172;</button>

<div id="chatWindow">
  <div class="chat-header">
    <div>
      <h4>Harini<span>AI</span></h4>
      <small>Ask me anything</small>
    </div>
    <button id="chatClose" aria-label="Close chat">&times;</button>
  </div>
  <div id="chatMessages"></div>
  <div class="chat-input-row">
    <input type="text" id="chatInput" placeholder="Type a message...">
    <button id="chatSend" aria-label="Send">&#10148;</button>
  </div>
</div>

<footer>
&copy; 2026 Harini Portfolio. All Rights Reserved.
</footer>

<script>
/* preloader */
window.addEventListener('load', () => {
  const pre = document.getElementById('preloader');
  setTimeout(() => pre.classList.add('hide'), 500);
});

const navbar = document.getElementById('navbar');
const scrollProgress = document.getElementById('scrollProgress');

window.addEventListener('scroll', () => {
  navbar.classList.toggle('scrolled', window.scrollY > 30);

  const h = document.documentElement;
  const scrolled = (h.scrollTop) / (h.scrollHeight - h.clientHeight) * 100;
  scrollProgress.style.width = scrolled + '%';

  document.getElementById('backToTop').classList.toggle('show', window.scrollY > 500);
});

const navToggle = document.getElementById('navToggle');
const navLinks = document.getElementById('navLinks');
navToggle.addEventListener('click', () => navLinks.classList.toggle('open'));
navLinks.querySelectorAll('a').forEach(a => a.addEventListener('click', () => navLinks.classList.remove('open')));

/* theme toggle */
const themeToggle = document.getElementById('themeToggle');
themeToggle.addEventListener('click', () => {
  document.body.classList.toggle('light');
  themeToggle.innerHTML = document.body.classList.contains('light') ? '&#9790;' : '&#9788;';
});

/* active nav link on scroll */
const sections = document.querySelectorAll('section[id]');
const navAnchors = document.querySelectorAll('.nav-links a');
const navObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if(entry.isIntersecting){
      navAnchors.forEach(a => a.classList.remove('active'));
      const link = document.querySelector('.nav-links a[href="#' + entry.target.id + '"]');
      if(link) link.classList.add('active');
    }
  });
}, { threshold: 0.5 });
sections.forEach(s => navObserver.observe(s));

/* typing effect */
const roles = ['Frontend Developer', 'UI / UX Designer', 'Creative Innovator', 'Problem Solver'];
const typedEl = document.getElementById('typedRole');
let roleIndex = 0, charIndex = 0, deleting = false;

function typeLoop(){
  const current = roles[roleIndex];
  if(!deleting){
    charIndex++;
    typedEl.textContent = current.slice(0, charIndex);
    if(charIndex === current.length){
      deleting = true;
      setTimeout(typeLoop, 1400);
      return;
    }
  } else {
    charIndex--;
    typedEl.textContent = current.slice(0, charIndex);
    if(charIndex === 0){
      deleting = false;
      roleIndex = (roleIndex + 1) % roles.length;
    }
  }
  setTimeout(typeLoop, deleting ? 40 : 80);
}
typeLoop();

/* stat counters */
const counters = document.querySelectorAll('.stat-number');
const counterObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if(entry.isIntersecting){
      const el = entry.target;
      const target = parseInt(el.dataset.count, 10);
      let current = 0;
      const step = Math.max(1, Math.ceil(target / 60));
      const tick = () => {
        current += step;
        if(current >= target){
          el.textContent = target;
        } else {
          el.textContent = current;
          requestAnimationFrame(tick);
        }
      };
      tick();
      counterObserver.unobserve(el);
    }
  });
}, { threshold: 0.4 });
counters.forEach(c => counterObserver.observe(c));

/* back to top */
document.getElementById('backToTop').addEventListener('click', () => {
  window.scrollTo({ top: 0, behavior: 'smooth' });
});

/* contact form validation */
const toast = document.getElementById('toast');
function showToast(msg){
  toast.textContent = msg;
  toast.classList.add('show');
  setTimeout(() => toast.classList.remove('show'), 3000);
}

document.getElementById('contactForm').addEventListener('submit', (e) => {
  e.preventDefault();
  const name = document.getElementById('nameField');
  const email = document.getElementById('emailField');
  const message = document.getElementById('messageField');
  const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  let valid = true;

  document.getElementById('nameError').classList.toggle('show', name.value.trim() === '');
  if(name.value.trim() === '') valid = false;

  document.getElementById('emailError').classList.toggle('show', !emailPattern.test(email.value.trim()));
  if(!emailPattern.test(email.value.trim())) valid = false;

  document.getElementById('messageError').classList.toggle('show', message.value.trim() === '');
  if(message.value.trim() === '') valid = false;

  if(valid){
    showToast('Message sent successfully.');
    e.target.reset();
  }
});

const revealEls = document.querySelectorAll('.reveal');
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if(entry.isIntersecting){
      entry.target.classList.add('in');
      observer.unobserve(entry.target);
    }
  });
}, { threshold: 0.15 });
revealEls.forEach(el => observer.observe(el));

/* testimonials slider */
const tSlides = document.querySelectorAll('.testimonial-slide');
const tDotsWrap = document.getElementById('testimonialDots');
let tIndex = 0;
let tTimer;

tSlides.forEach((_, i) => {
  const dot = document.createElement('button');
  dot.className = 'testimonial-dot' + (i === 0 ? ' active' : '');
  dot.setAttribute('aria-label', 'Go to testimonial ' + (i + 1));
  dot.addEventListener('click', () => goToTestimonial(i));
  tDotsWrap.appendChild(dot);
});
const tDots = document.querySelectorAll('.testimonial-dot');

function goToTestimonial(i){
  tSlides[tIndex].classList.remove('active');
  tDots[tIndex].classList.remove('active');
  tIndex = (i + tSlides.length) % tSlides.length;
  tSlides[tIndex].classList.add('active');
  tDots[tIndex].classList.add('active');
  resetTestimonialTimer();
}

function resetTestimonialTimer(){
  clearInterval(tTimer);
  tTimer = setInterval(() => goToTestimonial(tIndex + 1), 5500);
}

document.getElementById('testimonialNext').addEventListener('click', () => goToTestimonial(tIndex + 1));
document.getElementById('testimonialPrev').addEventListener('click', () => goToTestimonial(tIndex - 1));

if(tSlides.length){
  resetTestimonialTimer();
}

/* animated background — slow flowing gold waves */
const bgCanvas = document.getElementById('bgWaves');
if(bgCanvas){
  const bctx = bgCanvas.getContext('2d');
  let bw, bh;
  function resizeBg(){
    bw = bgCanvas.width = window.innerWidth;
    bh = bgCanvas.height = window.innerHeight;
  }
  resizeBg();
  window.addEventListener('resize', resizeBg);

  const waves = [
    { amp: 60, freq: 0.0022, speed: 0.00018, yRatio: 0.22, width: 1.1, alpha: 0.16 },
    { amp: 90, freq: 0.0016, speed: 0.00012, yRatio: 0.48, width: 1.4, alpha: 0.10 },
    { amp: 50, freq: 0.0028, speed: 0.00022, yRatio: 0.74, width: 1.0, alpha: 0.13 }
  ];

  function isLightBg(){ return document.body.classList.contains('light'); }

  function drawWaves(t){
    bctx.clearRect(0, 0, bw, bh);
    const gold = isLightBg() ? '156,122,47' : '201,162,75';

    waves.forEach((w, idx) => {
      bctx.beginPath();
      const baseY = bh * w.yRatio;
      for(let x = 0; x <= bw; x += 8){
        const y = baseY + Math.sin(x * w.freq + t * w.speed + idx * 2) * w.amp
                 + Math.sin(x * w.freq * 2.3 - t * w.speed * 1.4) * (w.amp * 0.25);
        if(x === 0) bctx.moveTo(x, y); else bctx.lineTo(x, y);
      }
      bctx.strokeStyle = `rgba(${gold},${w.alpha})`;
      bctx.lineWidth = w.width;
      bctx.stroke();
    });

    requestAnimationFrame(drawWaves);
  }
  requestAnimationFrame(drawWaves);
}

const trailCanvas = document.getElementById('trailCanvas');
if(window.matchMedia('(pointer:fine)').matches && trailCanvas){
  const tctx = trailCanvas.getContext('2d');
  let tw, th;
  function resizeTrail(){
    tw = trailCanvas.width = window.innerWidth;
    th = trailCanvas.height = window.innerHeight;
  }
  resizeTrail();
  window.addEventListener('resize', resizeTrail);

  let points = [];
  let mx = -999, my = -999, active = false;

  window.addEventListener('mousemove', (e) => {
    mx = e.clientX; my = e.clientY;
    active = true;
    points.push({ x: mx, y: my, life: 1 });
    if(points.length > 38) points.shift();
  });
  window.addEventListener('mouseleave', () => { active = false; });

  function isLight(){ return document.body.classList.contains('light'); }

  function drawTrail(){
    tctx.clearRect(0, 0, tw, th);

    points.forEach(p => { p.life -= 0.028; });
    points = points.filter(p => p.life > 0);

    if(points.length > 1){
      for(let i = 1; i < points.length; i++){
        const p0 = points[i - 1];
        const p1 = points[i];
        const lifeAvg = (p0.life + p1.life) / 2;
        if(lifeAvg <= 0) continue;

        const grad = tctx.createLinearGradient(p0.x, p0.y, p1.x, p1.y);
        const goldCore = isLight() ? '156,122,47' : '201,162,75';
        const goldHi = isLight() ? '122,94,34' : '232,214,168';
        grad.addColorStop(0, `rgba(${goldCore},${lifeAvg * 0.55})`);
        grad.addColorStop(1, `rgba(${goldHi},${lifeAvg * 0.85})`);

        tctx.strokeStyle = grad;
        tctx.lineWidth = Math.max(0.6, lifeAvg * 5.5);
        tctx.lineCap = 'round';
        tctx.lineJoin = 'round';
        tctx.shadowColor = `rgba(${goldCore},${lifeAvg * 0.6})`;
        tctx.shadowBlur = 12 * lifeAvg;

        tctx.beginPath();
        tctx.moveTo(p0.x, p0.y);
        tctx.lineTo(p1.x, p1.y);
        tctx.stroke();
      }
      tctx.shadowBlur = 0;

      if(active){
        const head = points[points.length - 1];
        const hg = tctx.createRadialGradient(head.x, head.y, 0, head.x, head.y, 10);
        const goldHi = isLight() ? '122,94,34' : '232,214,168';
        hg.addColorStop(0, `rgba(${goldHi},.9)`);
        hg.addColorStop(1, `rgba(${goldHi},0)`);
        tctx.fillStyle = hg;
        tctx.beginPath();
        tctx.arc(head.x, head.y, 10, 0, Math.PI * 2);
        tctx.fill();
      }
    }

    requestAnimationFrame(drawTrail);
  }
  drawTrail();
}


/* ambient floating gold particles */
const particleField = document.getElementById('particleField');
const particleCount = window.innerWidth < 768 ? 10 : 22;
for(let i = 0; i < particleCount; i++){
  const p = document.createElement('div');
  p.className = 'particle';
  const size = Math.random() * 3 + 1.5;
  p.style.width = size + 'px';
  p.style.height = size + 'px';
  p.style.left = Math.random() * 100 + 'vw';
  p.style.setProperty('--drift', (Math.random() * 80 - 40) + 'px');
  const duration = Math.random() * 14 + 14;
  p.style.animationDuration = duration + 's';
  p.style.animationDelay = (Math.random() * duration) + 's';
  particleField.appendChild(p);
}
/* ===== AI Chat Widget logic ===== */
const chatToggle = document.getElementById('chatToggle');
const chatWindow = document.getElementById('chatWindow');
const chatClose = document.getElementById('chatClose');
const chatMessages = document.getElementById('chatMessages');
const chatInput = document.getElementById('chatInput');
const chatSend = document.getElementById('chatSend');

const SYSTEM_PROMPT = `You are a friendly AI assistant on Harini's portfolio website.
Help visitors learn about Harini — a Frontend Developer / UI-UX Designer.
Answer questions about her skills, projects, experience, and how to contact her (email: harinitech001@email.com, location: Erode, Tamil Nadu, India).
Keep replies short, warm, and professional (2-4 sentences max). If you don't know something specific, suggest the visitor use the contact form below.`;

let chatHistory = [];
let chatOpened = false;

function addMessage(text, role){
  const el = document.createElement('div');
  el.className = 'chat-msg ' + role;
  el.textContent = text;
  chatMessages.appendChild(el);
  chatMessages.scrollTop = chatMessages.scrollHeight;
  return el;
}

function showTyping(){
  const el = document.createElement('div');
  el.className = 'chat-msg typing';
  el.id = 'typingIndicator';
  el.innerHTML = '<span></span><span></span><span></span>';
  chatMessages.appendChild(el);
  chatMessages.scrollTop = chatMessages.scrollHeight;
}

function hideTyping(){
  const el = document.getElementById('typingIndicator');
  if(el) el.remove();
}

chatToggle.addEventListener('click', () => {
  chatWindow.classList.toggle('open');
  if(!chatOpened && chatWindow.classList.contains('open')){
    chatOpened = true;
    addMessage("Hi! I'm Harini's AI assistant. Ask me about her skills, projects, or how to get in touch.", 'bot');
  }
  if(chatWindow.classList.contains('open')) chatInput.focus();
});

chatClose.addEventListener('click', () => chatWindow.classList.remove('open'));

async function sendChatMessage(){
  const text = chatInput.value.trim();
  if(!text) return;

  addMessage(text, 'user');
  chatHistory.push({ role: 'user', content: text });
  chatInput.value = '';
  chatSend.disabled = true;
  showTyping();

  try{
    const response = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        model: "claude-sonnet-4-6",
        max_tokens: 1000,
        system: SYSTEM_PROMPT,
        messages: chatHistory
      })
    });
    const data = await response.json();
    hideTyping();

    const reply = (data.content || [])
      .map(b => b.text || '')
      .join('\n')
      .trim() || "Sorry, I couldn't process that. Please try the contact form below.";

    addMessage(reply, 'bot');
    chatHistory.push({ role: 'assistant', content: reply });
  } catch(err){
    hideTyping();
    addMessage("Something went wrong. Please try again or use the contact form below.", 'bot');
  } finally {
    chatSend.disabled = false;
  }
}

chatSend.addEventListener('click', sendChatMessage);
chatInput.addEventListener('keydown', (e) => {
  if(e.key === 'Enter') sendChatMessage();
});

</script>

</body>
</html>
